import React from "react";
import { View, Text, StyleSheet } from "react-native";

const FilterScreen = (props) => {
  return (
    <View>
      <Text>yes it worked</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default FilterScreen;
